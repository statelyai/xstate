# Type-error-quality snapshot

Generated by `node scripts/type-error-quality.mjs` (tsc took 1.8s).
Each fixture contains exactly one realistic mistake; below is the complete
compiler output a user would see for it.

## eventNameTypo.ts

```
scripts/type-error-fixtures/eventNameTypo.ts(25,14): error TS2820: Type '"TOGLE"' is not assignable to type '"TOGGLE" | "SET_COUNT"'. Did you mean '"TOGGLE"'?
```

## guardNameTypo.ts

**NO ERROR REPORTED — the mistake compiles cleanly. This is a soundness gap.**

## targetTypo.ts

**NO ERROR REPORTED — the mistake compiles cleanly. This is a soundness gap.**
