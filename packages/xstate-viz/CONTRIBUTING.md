# Contributing

## Local development

To develop and test the `@xstate/viz` components in local projects:

1. Install dependencies:

```bash
yarn
```

2. Run Storybook:

```bash
yarn storybook
```

Good to go!
