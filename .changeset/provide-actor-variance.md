---
'xstate': patch
---

Registered invoke `onDone` callbacks now receive the invoked actor's output type, and `machine.provide({ actors })` accepts compatible actor sources with sound input/output variance.
