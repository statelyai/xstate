---
"xstate": patch
---

Support state names and effect keys such as `__proto__`, `constructor`, and `toString`, including persisted effect restoration. Run every attachment cleanup when an earlier cleanup throws, while preserving the original error.
