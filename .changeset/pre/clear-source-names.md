---
'xstate': major
---

Actor logic maps are named `actors` again. The aggregate `Implementations` type, `MachineImplementationsFrom` helper, and `machine.implementations` property are now `Sources`, `MachineSourcesFrom`, and `machine.sources`.
